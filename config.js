/* ==========================================================================
   SITE CONFIG — edit this file only to rebrand the site.
   Everything below is used everywhere in the page (header, footer, title,
   WhatsApp links, phone links, e-mail links).
   ========================================================================== */
window.SITE_CONFIG = {
  brand: {
    // Short lock-up shown next to the logo
    en: { short: 'TBM', full: 'TBM Maintenance' },
    ar: { short: 'TBM', full: 'TBM للصيانة' },
  },

  contact: {
    // Live contact number (info / response line).
    phoneDisplay: '+20 112 553 5943', // shown to visitors
    phoneDial: '+201125535943', // used in tel: links (no spaces)
    whatsappNumber: '201125535943', // international format, digits only, used in wa.me links
    email: 'supp.tbm@gmail.com',
  },

  // Accent colour is defined once in styles.css → --accent (see README).
};
